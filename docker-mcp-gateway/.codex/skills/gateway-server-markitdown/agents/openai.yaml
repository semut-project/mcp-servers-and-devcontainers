interface:
  display_name: "Gateway Server MarkItDown"
  short_description: "Convert documents to markdown/text"
  default_prompt: "Use the markitdown MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "markitdown"
      description: "Gateway Server MarkItDown MCP server"
