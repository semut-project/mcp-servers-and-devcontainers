interface:
  display_name: "Gateway Server FFmpeg"
  short_description: "Run media processing tasks"
  default_prompt: "Use the ffmpeg MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "ffmpeg"
      description: "Gateway Server FFmpeg MCP server"
