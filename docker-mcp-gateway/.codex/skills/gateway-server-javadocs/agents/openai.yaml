interface:
  display_name: "Gateway Server JavaDocs"
  short_description: "Look up Java APIs and symbols"
  default_prompt: "Use the javadocs MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "javadocs"
      description: "Gateway Server JavaDocs MCP server"
