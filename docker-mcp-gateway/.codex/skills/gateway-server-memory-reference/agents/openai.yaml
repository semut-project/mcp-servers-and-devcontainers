interface:
  display_name: "Gateway Server Memory Reference"
  short_description: "Store and retrieve contextual references"
  default_prompt: "Use the memory-reference MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "memory-reference"
      description: "Gateway Server Memory Reference MCP server"
