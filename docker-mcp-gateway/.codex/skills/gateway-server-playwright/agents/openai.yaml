interface:
  display_name: "Gateway Server Playwright"
  short_description: "Automate and inspect browser UI flows"
  default_prompt: "Use the playwright MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Gateway Server Playwright MCP server"
