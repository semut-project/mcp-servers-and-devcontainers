interface:
  display_name: "Gateway Server Context7"
  short_description: "Fetch up-to-date library docs"
  default_prompt: "Use the context7 MCP server for this task and report concise operational steps."

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Gateway Server Context7 MCP server"
